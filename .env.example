# Database (Neon/Vercel Postgres — free tier)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/numerae?sslmode=require"

# Auth.js — gere com: openssl rand -base64 32
AUTH_SECRET="change-me-to-a-long-random-string"
AUTH_URL="http://localhost:3000"

# E-mail (opcional — Resend free tier: 100 emails/dia)
# Sem isso, em dev o código aparece no terminal e na tela.
RESEND_API_KEY=""
EMAIL_FROM="Numerae <onboarding@resend.dev>"
